# AGC-in-Rust — Agent Guidance

The canonical guidance for this repository lives in dedicated docs:

- **Coding rules, embedded/no_std constraints, AGC cross-reference convention** → [`CLAUDE.md`](CLAUDE.md) (auto-loaded by all Claude Code agents).
- **Agent workflow, roles, hand-off flow, agent-selection triggers, parallelism rules** → [`docs/workflow.md`](docs/workflow.md).
- **Simulator (`agc-sim`) spec** → [`docs/simulation.md`](docs/simulation.md).

This stub exists so cross-tool agents (Codex, Cursor, etc.) that look for `AGENTS.md` are redirected to the canonical docs above.
