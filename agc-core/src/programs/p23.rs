//! P23 — Cislunar midcourse navigation (star/landmark sightings).

use crate::executive::job::JobPriority;

pub fn init(state: &mut crate::AgcState) -> JobPriority { let _ = state; todo!("P23 init") }
