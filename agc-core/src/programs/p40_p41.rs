//! P40 — SPS thrusting program.
//! P41 — RCS thrusting program.

use crate::executive::job::JobPriority;

pub fn init_p40(state: &mut crate::AgcState) -> JobPriority { let _ = state; todo!("P40 init") }
pub fn init_p41(state: &mut crate::AgcState) -> JobPriority { let _ = state; todo!("P41 init") }
