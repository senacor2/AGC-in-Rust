//! P30 — External Delta-V targeting (ground-uploaded maneuver).

use crate::executive::job::JobPriority;

pub fn init(state: &mut crate::AgcState) -> JobPriority { let _ = state; todo!("P30 init") }
