// This file is the default binary for `cargo run -p agc-sim`.
// The full TUI simulator lives in `src/bin/agc_sim.rs`.
// Run it with: cargo run -p agc-sim --bin agc_sim
fn main() {
    eprintln!("Use `cargo run -p agc-sim --bin agc_sim` to start the simulator.");
}
